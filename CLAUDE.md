# CLAUDE.md

## 1. 目录定位

本目录是 **LM3-UP 复合机器人资料与 SDK Git 仓库**。整机资料分别列出上海乐白 LM3 协作机械臂、LMG-90 夹爪、复合机器人控制箱、RK3588S2 应用主机、“鲁班猫 0wifi”无线视觉模块、RGB 摄像头、云迹 UP 移动底盘和收纳柜等部件；不要在未核对实物接线前把应用主机与无线视觉模块合并视为同一设备。

- `LM3-UP复合机器人开发资料/` 保存原始交付 PDF、DOCX、LBD 和 ZIP，不在整理过程中改写。
- `docs/` 是针对 LM3-UP 整理的开发、SDK、VLA 和真机验收文档。
- `vendor/` 使用 Git 子模块固定上游乐白 SDK/ROS/插件以及云迹相关公开资料。
- `scripts/manage-sdks.ps1` 负责可复现初始化和验证子模块；不要直接对全仓执行无差别递归初始化。
- `examples/read_lm3_status.py` 是默认只读示例，不启动系统或发送运动。
- 当前仍没有完成覆盖 LM3 机械臂与 UP 底盘的整机运行时、WATER 生产客户端、整机级安全协调器或端到端自动化测试；现有 `teleop/bridge` 只是默认仿真的机械臂侧安全桥，“仓库有 SDK/桥接器”不等于“已经真机集成”。
- 根目录 `LM3-UP复合机器人开发资料.zip` 与展开目录内的 11 个文件逐项 SHA-256 一致，是归档快照，不是另一版本。
- `apriltag.zip` 与 `scene-export-20240801172704538000/` 下的 3 个 `.lbd` 逐字节一致；前者用于整体导入，后者用于审阅具体场景。
- 本文件总结的是资料中的出厂默认值和示例。**尚未据此证明任何现场机器人的当前 IP、固件版本、地图、标定值或实际运行状态。**

以后处理此目录时，默认保持原始 PDF、DOCX、LBD 和 ZIP 不变；除非用户明确要求，不要重打包、覆盖、改名或重写这些交付文件。`vendor/` 是上游只读快照：修复应进入可访问的 fork 或主仓独立适配层，不要创建只存在本机、远端无法取得的子模块提交。

## 2. 资料索引与使用优先级

日常开发先阅读 `docs/README.md`、`docs/LM3-UP开发指南.md` 和 `docs/SDK与子模块.md`；本文件保留更完整的手册事实、冲突项与安全规则。

| 文件 | 页数/形式 | 主要用途 |
| --- | ---: | --- |
| `1.LM3UP复合机器人简明手册-2025.pdf` | 11 页 | 整机组成、网络拓扑、整机参数、快速开关机/建图/抓取入口 |
| `2.云迹up底盘硬件使用说明.pdf` | 10 页 | 底盘部件、按键、灯态、维护、电池、安全和故障排查 |
| `3.云迹up底盘软件使用指南.pdf` | 14 页 | 9001 监控页、建图、修图、点位、禁行线、充电桩和续扫 |
| `4.RK摄像头使用指南.pdf` | 7 页 | RK 插件、相机标定、手眼标定、AprilTag 和场景导入 |
| `water_api手册.pdf` | 62 页 | 云迹 WATER TCP API v1.8.9 |
| `apriltags-印刷.pdf` | 4 页 | `tag36h11` 不同尺寸与 ID 的打印版 |
| `反光贴纸定位使用.docx` | 2 页 DOCX，10 段、2 图 | 反光贴精确停靠的材料、尺寸、安装和点位参数 |
| `scene-export-20240801172704538000/*.lbd` | 3 个 JSON/Lua 场景 | AprilTag 检测、偏移标定和抓取示例 |

事实冲突时按以下顺序处理：

1. 人身、电气和机械安全优先采用多份资料的严格交集。
2. 整机专用操作优先参考 2025 简明手册；底盘内部细节参考云迹硬件/软件手册。
3. API 字段以 `water_api手册.pdf` 的具体接口页为准，但必须先核对现场 WATER 软件版本。
4. 视觉流程以 RK 指南为主；实际场景逻辑以 `.lbd` 的 `lua_source` 为准。
5. 截图中的 IP、版本、标签 ID、标定值和偏移量都只是示例，不能直接当成现场真值。
6. 产品实物、现场 UI 和实测结果与文档不一致时，停止动作并记录差异，不要自行猜测。

## 3. 不可绕过的工作规则

### 3.1 未经明确授权不得执行的操作

- 让底盘或机械臂运动、进入/解除急停、抓取物体。
- 修改机器人网络、网关、Wi-Fi、路由器配置或默认口令。
- 覆盖/切换地图、创建同名同楼层地图、删除点位、删除禁行线。
- 重启软件、整机断电重启、关机、检查更新、软件更新或硬件更新。
- 导入或改写 `.lbd` 场景、修改标定参数、写入抓取偏移。
- 测量带电充电触片；此项只允许具备相应电气能力的人员执行。

### 3.2 任何运动前都必须确认

1. 现场有人监护，能够直接触达物理急停。
2. 机械臂和底盘工作区清空；人员、头发、衣物、线缆和易碎物远离运动部件。
3. 机器人位于室内、平整、干燥地面，不在坡道、楼梯、悬空边缘或自动扶梯附近。
4. 地图、楼层、当前位置和激光数据与现场吻合。
5. 软急停和硬急停状态均已读取并与现场一致。
6. 电量、充电状态和 `error_code` 正常；非零错误码不允许带病运行。
7. 先使用最低可行速度和无负载/空抓测试，再逐步放开范围。
8. API 返回“已接收”不等于动作完成；必须等待任务状态和现场运动均结束。

### 3.3 保守环境边界

资料存在规格差异，复合系统统一采用更严格边界：

- 工作温度：`0-40 ℃`。
- 相对湿度：`10%-70%`。
- 仅室内平整地面。
- 可通行净宽：至少 `600 mm`。
- 充电桩正前方至少 `1.5 m` 无障碍，且不得位于斜坡。
- 禁止在存在易燃气体、蒸汽、液体、粉尘或纤维的环境运行，也不得运输易燃易爆物品。
- 沟宽上限文档值为 `40 mm`。最大过坎 `20 mm` 只对应“不含舱架、负载 50 kg”的底盘条件；完整 LM3-UP 不得据此认定可安全过坎。
- 坡度上限 `12°` 只对应“不含舱架、负载 20 kg”的底盘条件；完整 LM3-UP 不得据此认定可安全爬坡，实际部署应避免坡道。
- 不得在坡道按急停或手推机器人，失去驱动力后可能下滑。

## 4. 系统概览

| 组件 | 文档中的关键规格 |
| --- | --- |
| LM3 协作机械臂 | 6 轴，负载 3 kg，工作半径 638 mm，自重 9.5 kg，IP54，末端 `DI×2/DO×2` |
| LMG-90 夹爪 | 行程 0-90 mm，夹持力 10-35 N |
| 复合机器人控制箱 | 整机资料列为独立部件；本文档未给出内部电气、端口和维护细节 |
| RK3588S2 应用主机 | 4×A76 + 4×A55，NPU 6T；负责应用与视觉插件 |
| 鲁班猫 0wifi 无线视觉模块 | 整机资料列为独立部件；与 RK 主机的现场连接关系需按实物确认 |
| RGB 相机 | UVC USB，720P，90°无畸变；整机资料称可安装于机械臂或移动底盘，本资料的手眼/抓取流程采用机械臂末端安装 |
| UP 底盘 | 60 kg，最大 1.5 m/s，正常 0.8-1.4 m/s，离地 40 mm，20 m 激光、超声、前后/俯仰视觉和红外传感器 |
| 收纳柜 | 整机资料列为交付组成，未给出可承载质量或内部安装细节 |
| 整机 | 待机约 535×450×1200 mm；AC 220 V/50 Hz；24 V 16 Ah 电池 |

底盘普通定位规格为约 `5-15 cm / 8°`，精定位约 `2 cm / 3°`。这不是对任何现场地图或贴纸停靠精度的保证，部署后仍须实测。

## 5. 默认网络拓扑与访问入口

以下均为交付文档默认值，现场可能已修改。默认口令属于敏感信息，不要写入外部日志、截图、工单或公开仓库；投产前应改密。

| 对象 | 默认地址/SSID | 默认凭据或说明 |
| --- | --- | --- |
| 复合机器人路由器 | `192.168.10.240`；SSID `LB-AMR-xxxx` | 凭据见设备标签/原始手册；投产前改密 |
| LM3 有线控制页 | `http://192.168.10.200` | 凭据见设备标签/原始手册；投产前改密 |
| LM3 直连 Wi-Fi | SSID `Lebai-XXXXX`；`10.20.17.1` | 凭据见设备标签/原始手册；投产前改密 |
| UP 底盘主机 | `192.168.10.10` | 控制网静态地址 |
| UP 监控/建图页 | `http://192.168.10.10:9001` | 文档未给登录账户 |
| UP 直连 Wi-Fi | SSID `yunji-WT-xxxxx` | 凭据见设备标签/原始手册；投产前改密 |
| WATER TCP API | `192.168.10.10:31001` | TCP socket，不是 HTTP REST |
| RK 应用主机/插件页 | `http://192.168.10.201/ui/plugin` | 文档未给系统/SSH账户 |

网络操作规则：

- 先从路由器租约、9001 状态栏或现场标签发现实际地址，再访问设备。
- `192.168.10.10:9001` 是网页工具；`192.168.10.10:31001` 是 TCP API，不能混用。
- 只做内网调试时，不需要给电脑设置默认网关。需要经复合路由器上外网时，整机手册建议电脑网关为 `192.168.10.240`。
- 底盘软件手册截图中的 `192.168.10.1` 是机器人有线配置示例，与“调试电脑经复合路由器上网时使用 `192.168.10.240` 作为网关”不是同一配置场景；两者都不得直接照抄，必须先发现现场拓扑。
- RK 指南后文出现 `192.168.22.200` 的机械臂 URL，而整机拓扑给出 `192.168.10.200`；资料未解释二者是否来自不同接口或路由。应把它们视为未协调的地址示例，现场发现接口、路由和实际服务入口后再使用。
- RK 指南截图显示 `192.168.4.40`，交付 LBD 默认值为 `192.168.4.46`；二者都是当时 signal 模式的示例地址，不代表当前 RK 地址。
- 文档没有 SSH 用户、系统密码、TLS、Token 或 API 鉴权信息，不要臆造。

## 6. 整机操作与底盘安全

### 6.1 开关机

完整 LM3-UP 优先使用整机手册流程：

- 开机：打开总电源，按开机按钮；“两短一长”表示开始启动。底盘绿灯持续闪烁为启动中，底盘和机械臂白灯常亮后才视为完成。
- 关机：长按开机按钮，听到“七短一长”后，再关闭总电源。
- 底盘单体手册写“听到长响时松开”，并要求关机 30 秒后再重启；实机提示不一致时以设备标签和受训人员流程为准。
- 安装/拆卸夹爪、相机或线缆前，必须完成关机、断开总电源并确认全部运动停止。

### 6.2 急停

- 整机示意图有一个停止全部运动的急停和一个只停止 UP 底盘的急停；操作前应以现场标识确认具体按钮。
- WATER 的 `/api/estop` 只控制软急停；物理硬急停与软急停相互独立，软件不能解除硬急停。
- 软件或整机重启会把软急停重置为 `false`，因此软急停不是可持久依赖的安全锁。
- 急停后底盘失去驱动力。只允许在必要标定/排障时，由受训人员在平整地面缓慢推行；禁止日常随意推动。

### 6.3 灯态冲突

两份手册对灯态有冲突：

- 整机简明手册称“白灯闪烁”为充电中。
- 底盘硬件手册称“白灯闪烁”为急停，“白灯呼吸”为充电。

不得仅凭灯态判断安全状态；必须同时查看 9001 或 `/api/robot_status` 的充电、软急停、硬急停字段，并现场核对固件行为。

### 6.4 充电与维护

- 自动触片无法充电时，可使用充电桩侧面的手动充电线；先确认接头、极性和设备说明。
- 低于 10% 电量前应充电；充电出现异味或异响立即停止。
- 充电桩应安装在平整硬质地面、靠墙牢固固定并可靠接地，不得随意移动；桩体周围和充电位不得堆放物品或被占用。
- 超过 30 天不用：关闭底盘电池开关，保持 40%-60% 电量，至少每 3 个月补充充电 0.5-1 h；不要长期停在充电桩上。
- 激光/相机用软无纺布或眼镜布清洁；超声防护网可用牙签/镊子轻理毛屑，但不得触碰中心。
- 禁用 84、洗涤灵、洁厕剂等腐蚀剂。充电触片顽固污渍可在断电后，用蘸少量 95% 以上酒精的布清洁。

## 7. 9001 监控、建图和导航部署

### 7.1 先看状态

`http://192.168.10.10:9001` 应能查看：地图/楼层、控制模式、电量/充电、软硬急停、软件版本、Wi-Fi IP 和序列号。

位置偏移时：打开激光 -> 进入校正 -> 拖拽机器人姿态 -> 让红色激光/辅助线与墙面等实物吻合 -> 完成后立即退出校正模式。移动任务执行中一般不要调用 API 强制校正。

### 7.2 点位类型

| 类型 | 含义/规则 |
| ---: | --- |
| `0` | 普通点位 |
| `1` | 前台点（API 手册常见类型） |
| `3` / `4` | 电梯外 / 电梯内 |
| `7` | 闸机点 |
| `11` | 充电桩点位；按当前软件指南，每张地图只配置一个活动回充点 |
| `13` | 反光贴参照物精确停靠点 |
| `76` | 减速区域点位 |

API 自定义点位类型应大于 `1000`，避免与厂商后续类型冲突。非普通点优先在 9001 UI 中创建，以免漏掉类型专属属性。

减速区命名格式为 `名称_距离`，距离单位为分米；示例 `_10` 表示 1 m 双向，`_21` 表示 2.1 m 单向，默认减速至约 0.4 m/s。点位方向应垂直行走方向。

### 7.3 禁行线与地图的破坏性行为

- 同名同楼层创建地图会覆盖旧地图。
- 禁行线重名会自动删除原有同名线。
- 修改机器人当前使用地图后，必须重启 WATER 软件服务，修改才会生效；重连后还必须重新确认当前地图。
- 执行这些操作前必须导出/备份地图并获得用户确认。

禁行线在规划中等价于实体墙。必须覆盖或隔离：透明玻璃、楼梯/台阶、悬空边缘、低矮障碍、机器人无法识别的边角，以及中间镂空的桌椅/长椅。最终地图外圈也要合理封闭。

### 7.4 建图流程

1. 确认电量充足、网络稳定和环境清空。
2. `地图管理 -> 创建地图`，输入唯一地图名和楼层。
3. 选择地图尺寸：小图 `<3600 m²`，中图 `3600-10000 m²`，大图 `>10000 m²`。
4. 等待激光数据出现；建图期间普通定位页不再显示机器人位置。
5. 使用 `I/J/K/L` 低速驾驶；操作者在底盘后方保持安全距离和清晰视线，避免站在前方遮挡激光，禁止贴近尾随。
6. 扫描完成后保存；修图时清除人员、椅子等临时障碍和路径中的无效灰区。
7. 用禁行线封闭外圈及所有危险区域。
8. 创建普通点、充电点、减速点/精停点。
9. 如果修图的是机器人当前使用地图，必须重启 WATER 软件服务；重连后重新确认当前地图、楼层和定位状态。
10. 完成低速跑圈、障碍测试和自动回充；仅“页面保存成功”不算部署完成。

续扫只适用于由 `0.8.2` 及以后软件创建、且仍由原机器人持有的地图。复制到另一台机器人或由更老版本创建的地图不能据此续扫。

### 7.5 充电桩标定

1. 断开充电桩电源，避免自动位置校正。
2. 将机器人与触片充分对齐。
3. 在 9001 页面精确校正位置。
4. 创建/确认类型 `11` 的充电桩点位。
5. 充电桩上电，执行自动回充测试。
6. 回充后再次检查激光与实际场景吻合。

充电桩正前方 `1.5 m` 不得有障碍。面对面充电桩间距应大于 `1.5 m`，平行放置应大于 `0.8 m`。硬件手册虽写“一机多桩”，当前软件指南仍限制每台机器人/每张地图一个已标定回充点；多桩必须另行验证调度和固件支持。

## 8. WATER TCP API

### 8.1 协议事实

- 手册版本：`v1.8.9`，修订日期 `2022-12-26`。
- 连接：默认静态控制网入口为 TCP client -> `192.168.10.10:31001`。手册也允许底盘接入环境 Wi-Fi 后，使用查询到的局域网 IP 连接同一端口，因此该静态地址不是唯一可能入口。
- 请求：发送类 URL 字符串，如 `/api/move?marker=A&uuid=...`。
- 响应：JSON；`type` 为 `response`、`callback` 或 `notification`。
- `uuid` 是客户端关联 ID，响应原样返回；移动接口另返回服务端 `task_id`。
- 手册没有规定编码、消息终止符、粘包/拆包、最大包长、超时、心跳或重连策略。实现时不得假设“一次 `recv` 就是一条 JSON”，必须通过现场抓包/受控测试确认分帧并使用缓冲解析器。
- 手册未说明 TLS、Token 或应用层登录鉴权；Wi-Fi 密码还会以明文查询参数发送。应按未鉴权明文接口进行隔离防护，并现场确认实际版本的能力。
- notification 可能因网络原因丢失，不能作为唯一状态机依据。

典型响应：

```json
{
  "type": "response",
  "command": "/api/...",
  "status": "OK",
  "error_message": "",
  "uuid": "...",
  "results": {}
}
```

通用 `status`：`OK`、`INVALID_REQUEST`、`REQUEST_DENIED`、`UNKNOWN_ERROR`、`GOAL_CAN_NOT_BE_REACHED`。`error_message` 可能为空且内容可能变化，不能按固定文案驱动流程。

### 8.2 接口分组

| 目的 | 接口 |
| --- | --- |
| 基本信息/状态 | `/api/robot_info`、`/api/robot_status`、`/api/request_data`、`/api/get_current_location` |
| 导航移动 | `/api/move`、`/api/move/cancel` |
| 直接控制/急停 | `/api/joy_control`、`/api/estop` |
| 点位 | `/api/markers/insert`、`/api/markers/query_list`、`/api/markers/delete`、`/api/markers/count`、`/api/markers/query_brief`、`/api/markers/insert_by_pose` |
| 定位校正 | `/api/position_adjust`、`/api/position_adjust_by_pose` |
| 参数 | `/api/set_params`、`/api/get_params` |
| Wi-Fi | `/api/wifi/list`、`/api/wifi/connect`、`/api/wifi/get_active_connection`、`/api/wifi/info`、`/api/wifi/detail_list` |
| 地图 | `/api/map/list`、`/api/map/set_current_map`、`/api/map/get_current_map`、`/api/map/list_info`、`/api/map/accessible_point_query`、`/api/map/distance_probe` |
| 系统生命周期 | `/api/shutdown`、`/api/software/get_version`、`/api/software/check_for_update`、`/api/software/update`、`/api/software/restart` |
| 诊断/电源 | `/api/diagnosis/get_result`、`/api/get_power_status` |
| 路径/电梯 | `/api/get_planned_path`、`/api/lift_status`、`/api/make_plan` |
| 灯带 | `/api/LED/set_luminance`、`/api/LED/set_color` |

### 8.3 移动状态机

`move_status`：`idle`、`running`、`succeeded`、`failed`、`canceled`。

`running_status` 可能包括：`idle`、`leave_charging_pile`、`dock_to_charging_pile`、`leave_container`、`dock_to_container`、`leave_cabin`、`dock_to_cabin`、`goto_lift`、`wait_lift_unlock`、`wait_lift_outside`、`enter_lift`、`avoid_lift`、`take_lift`、`exit_lift`、`back_to_lift`、`running`。

在 `enter_lift`、`avoid_lift`、`take_lift`、`exit_lift`、`back_to_lift` 阶段不要随意 cancel。

普通移动推荐顺序：

1. 连接 TCP。
2. 查询软件版本、当前地图和机器人状态。
3. 确认 `running_status=idle`、地图正确、软硬急停解除、错误码为零。
4. 订阅 `robot_status` callback，或以 1-2 Hz 轮询 `/api/robot_status`。
5. 发送 `/api/move`，保存 `uuid` 和 `task_id`。
6. 使用 `move_status/running_status` 控制流程，notification 只做辅助提示。
7. 取消后等待状态回到 `idle`，再发送下一任务。

单点 `/api/move`：

- `marker` 或 `location=x,y,theta` 二选一；同时存在时 `location` 优先。
- `max_continuous_retries` 默认 30。
- `distance_tolerance`、`theta_tolerance` 默认值依机型。
- `angle_offset` 范围约 `[-3.14, 3.14]` rad。
- `yaw_goal_reverse_allowed` 控制双向机器人的停靠方向。
- `occupied_tolerance` 是目标被占用时的让步停靠距离。

巡游 `/api/move?markers=...`：至少两个点，不支持跨楼层；`count=-1` 为无限循环；单点失败后仍会继续下一点，结束仍用 `/api/move/cancel`。

### 8.4 关键接口陷阱

- `/api/joy_control` 优先级高于导航 move。线速度约 `[-0.5,0.5] m/s`，角速度约 `[-1.0,1.0] rad/s`；每条命令只持续 0.5 秒。遥控客户端必须有本地看门狗，失去输入时立即停止续发并发送零速。
- `/api/set_params` 即使设置失败也可能返回 `status=OK`；必须用 `/api/get_params` 回读。设置仅持续到软件/整机重启。
- `/api/request_data` 本身未标记为弃用；仅历史参数 `switch=on/off` 被标记为旧版本弃用参数。当前示例用必选 `topic` 和可选 `frequency` 开启 callback，但手册没有定义同一 TCP 连接内停止某个 topic、重复请求、修改频率或断线后服务端清理订阅的语义，不得假设新版本仍支持 `switch=off`。
- `/api/map/set_current_map` 会重启 WATER 服务，可能收不到响应；重连后再查询当前地图和点位。
- `/api/software/update`、`restart` 和关机/重启会断开 socket；不得在未获授权时调用。
- `/api/get_planned_path` 无任务时返回空路径，最多返回约 2000 个点。
- `/api/lift_status` 只在等梯至出梯流程中有效，其他时段可能超时。
- `/api/get_power_status` 中电池电流正数表示充电、负数表示放电。
- LED 颜色写硬件 flash，不应高频调用；急停/充电状态下可能返回成功但实际不生效。
- `robot_status.error_code` 非零代表异常，但资料没有完整位定义。
- notification 会广播给所有已连接客户端，但手册没有说明多个控制客户端的所有权、优先级或并发写入仲裁。保守实现应让每台机器人只有一个运动命令写入者，尤其不得让 `joy_control` 与导航 move 由不同客户端并发控制。

API 手册的若干示例不是可直接复制的严格 JSON：含注释、占位 `bool`、大小写错误、缺逗号、裁切字段和错误 command。实现测试报文时按字段语义重写，不要整段复制。

## 9. RK 相机、标定和 AprilTag

### 9.1 插件与示例配置

相机固定在机械臂末端，USB 接入 RK 板，线缆沿机械臂可靠固定并保留关节运动余量。按指南在插件页把“真机在线”切换为“离线仿真”，再安装并启用 3 个插件；更新插件时应先关闭使能：

1. `camera`
2. `camera_calibrater`
3. `apriltag`

文档截图示例，不是强制现场值：

| 项目 | 示例 |
| --- | --- |
| camera | 索引 0，1280×720，不翻转 |
| 棋盘格 | 角点行 8、角点列 11、角点间距 0.02 m |
| AprilTag | `tag_family=tag36h11`、`tag_size=0.032 m`、`signal=13` |

实际参数必须与相机枚举、标定板和打印标签的物理尺寸一致。

### 9.2 标定顺序

相机内参：机械臂保持不动，移动棋盘格到不同位置；彩色角点稳定后记录，通常采集 15-30 个姿态，再执行标定并确认内参/畸变结果出现、记录数归零。采集期间保持标定板方向标记一致，避免超过约 45° 的大幅倾斜导致方向翻转。

手眼标定：棋盘格保持不动，选择“眼在手上”，填写实际机械臂 IP；示教机械臂到不同位置和角度，幅度不要过大，同样采集 15-30 个姿态。完成后确认“相机位置”参数出现、记录数归零。

AprilTag：配置 `tag_family`、以米为单位的实际 `tag_size` 和通信方式；标签放入视野后先在插件页验证 ID 与位姿，再进入机械臂场景。资料没有定义输出位姿的坐标系、平移/旋转单位、欧拉角顺序或多标签同 ID 的处理方式，接入代码前必须用受控位姿实验确认。

“结果字段出现、记录数归零”只是流程完成，不是精度验收。若插件能提供重投影或手眼误差，应保存这些指标；若不能，则必须用受控位姿和重复定位实验形成误差、覆盖姿态与重复性证据。

### 9.3 打印标签

`apriltags-印刷.pdf` 版面标为 `36h11`，插件配置字符串示例为 `tag36h11`。印刷文件没有文字图例正式定义数字分组的单位；结合版面标注和插件 `0.032 m` 示例，可以推断这些分组对应约 20/32/40/80 mm 的标签尺寸，但使用前必须实测确认。版面分组为：

- 标注 20：ID 0-23。
- 标注 32：ID 0-15。
- 标注 40：ID 0-4、10-14。
- 标注 80：ID 50、60、80、90。

打印时使用实际大小/100%，禁止“适应页面”缩放；打印后保留外围白色静区，并实测、确认插件所采用的物理边界后，再把对应米制值写入 `tag_size`。资料没有正式定义 `tag_size` 应量取黑色编码区还是其他边界，不能只凭文件名或目测决定。`tag_id` 必须与插件实际识别结果一致。

相同 family+ID 在不同尺寸中会重复，版面中还存在重复图案但用途未说明，而插件一次只配置一个 `tag_size`。不要让相同 ID 的两个标签同时进入视野，也不要在同一识别任务中混用同 ID 的不同物理尺寸，否则会产生身份或尺度歧义。

## 10. `.lbd` 场景说明

`.lbd` 是 UTF-8 JSON，实际程序位于 `lua_source`。导出快照来自 LM3，场景格式 1.4，元数据记录 `ds_version=2.4.20+24061317`、`rc_version=3.1.33+24080108`；这些只是导出时版本。

| 文件/场景 ID | 作用 | 关键行为 |
| --- | --- | --- |
| `apriltag-10253.lbd` / `10253` | 公共标签检测模块 | 支持 `signal` 和法兰 `modbus`；默认 `way="modbus"`，`run_test=false` |
| `apriltag-offset-10243.lbd` / `10243` | 示教 tag 到抓取点的偏移 | 默认 `tag_id=11`；先到标签前方 0.2 m，再进示教，肩部 DI 确认后打印 offset |
| `抓标签-10252.lbd` / `10252` | 抓取示例 | 默认 `tag_id=11`，使用一次具体标定得到的 `cell2tag`；抓取后发起返回起始 TCP 的运动，但完成语义需现场验证 |

后两个场景用 `scene("10253")` 依赖数值 ID。导入后若 ID 被重映射，调用会失效；导入时必须核对场景 ID 和依赖关系。RK 指南截图还出现一个 `apriltag-modbus` 场景，但它不在本资料的 `apriltag.zip` 中，不能假定已经交付。

### 10.1 signal 与 Modbus 必须成对配置

RK 指南要求把 `way` 改为 `signal`，但交付的 `apriltag` 场景默认是 `modbus`。两种方式都可用，但插件与 Lua 必须选择同一种：

- signal：只有选择此模式时，才把 `camera_ip` 改成实际 RK 地址，并让 `sid` 与插件 signal 一致；触发后读取 `plugin_apriltag_tags`。
- Modbus：使用法兰 Modbus、slave `0x07`。寄存器 `300` 为触发/状态，`301` 为标签数量，`302-307` 写入当前法兰位姿；标签结果从 `311` 起、每个标签跨 10 个寄存器，读取 ID + 6D 位姿。

Modbus 位姿编码：xyz 乘 1000 转毫米；角度按一圈映射到 0-65535，顺序为 x、y、z、rz、ry、rx。

### 10.2 场景风险

- signal/Modbus 轮询没有超时，插件异常会永久阻塞。
- 读取长度、JSON 结构和设备状态缺少校验，且大量 Lua 变量是全局变量。
- 偏移场景的前置移动失败后仍可能进入示教；肩部按钮等待也无超时。
- `offset` 不会自动写入抓取场景，必须人工复制并复核。
- `cell2tag` 是一次具体安装/标定的结果；相机、夹爪、TCP 或标签安装变化后必须重标，禁止复用旧值。
- 抓取示例没有安全接近点、碰撞/夹取成功检测、失败退避或重试。`set_claw(100, 0)` 后没有 `sync()` 就立即发起回撤，而脚本已关闭自动同步，夹爪可能尚未闭合完成便开始移动；最后返回起始 TCP 的运动后也没有显式 `sync()`，不能据此确认回撤已完成。
- 这些场景只能在现场监护和低速空载验证后使用，不能直接作为无人值守生产程序。

偏移标定时，指南要求 `good_pose` 与 `verify` 的位置差在 2 mm 内，再复制 offset，但没有说明是逐轴限制还是距离范数，也没有定义旋转误差。公共检测场景的 `run_test` 分支硬编码测试 `tag_id=1`，不能证明抓取场景默认的 ID 11 可用；如经授权启用测试，必须先核对目标 ID，并确认返回结果确实包含生产标签。运行抓取前把 `run_test` 恢复为 `false`，确认 tag ID、实际通信方式和所有偏移量。

## 11. 反光贴精确停靠

材料：5 cm 宽反光贴条和 5 cm 宽遮光海绵。

贴纸图案总尺寸约 `55 cm × 5 cm`，横向从左到右为：

```text
20 cm 反光 + 6 cm 黑色 + 3 cm 反光 + 6 cm 黑色 + 20 cm 反光
```

安装要求：

- 贴在柜体、桌体或墙面的侧面。
- 贴纸中轴线与底盘中轴线对齐。
- 图示标注贴纸距地约 25 cm，尺寸线看似量到贴纸下缘，但原文没有正式定义测量基准；现场制作时必须确认并记录。
- 地图中在贴纸附近创建类型 `13` 的停靠点，箭头方向朝外。
- 根据实际从正面还是背面靠近选择对接方向。
- `prepare_offset` 示例/默认 `1.05`，`align_offset` 示例/默认 `0.9`。
- `final_offset` 必须填写底盘中心点到贴纸的实测距离；图中示例为 `0.5`，不能照抄。
- DOCX 没有写明三个 offset 的单位、坐标方向或验收误差；虽然数值形态很像米制距离，也必须以现场 UI/固件说明和实测为准。

验收不能以一次成功停靠结束。应在预期环境光、目标材质、正反靠近方向和允许的初始姿态范围内重复测试，记录最终位置/角度误差、成功率、失败判据和安全停止行为；在确认传感器识别原理与限制前，不得假定反光贴在所有光照和材质环境下都可靠。

## 12. 已知文档冲突与保守结论

| 主题 | 文档差异 | 本目录的执行结论 |
| --- | --- | --- |
| 最小通道 | 55 cm / 600 mm | 使用 `≥600 mm` |
| 温度 | 整机 -10-40 ℃；底盘 0-50 ℃；电池 0-40 ℃ | 使用 `0-40 ℃` |
| 充电灯态 | 白闪=充电 / 白闪=急停、白呼吸=充电 | 同时读状态字段并现场确认 |
| 手推 | 急停后可推 / 一般禁止推动 | 仅必要标定/排障、平地、受训人员、已急停时缓慢推 |
| 充电桩数量 | 一机多桩 / 每地图一个充电点 | 默认一个活动回充点，多桩另做版本验证 |
| 网络地址示例 | 调试电脑上网网关 `192.168.10.240` / 机器人有线配置截图 `192.168.10.1` | 用途不同，不构成同一参数的直接冲突；先发现现场拓扑，不直接修改 |
| LM3 地址 | `192.168.10.200` / RK 指南 `192.168.22.200` | 视为未协调示例；发现接口、路由和实际服务入口后使用 |
| RK 地址 | 拓扑 `192.168.10.201` / 指南截图 `192.168.4.40` / LBD `192.168.4.46` | 只有 signal 模式使用 `camera_ip`；此时必须改为实际 RK 地址，Modbus 模式不使用该字段 |
| AprilTag 通信 | 指南推荐 signal / LBD 默认 Modbus | 插件和 Lua 明确选定同一种方式 |
| 充电时间 | 关机约 3.5 h、待机约 5.5 h / 底盘 ≤4 h | 视负载和状态而定，不承诺固定时长 |

## 13. 验证与交付标准

### 导航/地图

- 记录实际底盘 IP、软件版本、地图名和楼层。
- 地图外圈与玻璃/楼梯/悬空/镂空危险区均已安全封闭。
- 点位类型、方向和命名正确；充电点、减速区、精停点分别验证。
- 反光贴精停在预期光照、材质、靠近方向和初始姿态范围内做重复测试，记录位置/角度重复性、成功率、失败判据和停止行为。
- 激光数据与现场吻合。
- 完成低速跑圈、障碍物测试、取消任务和自动回充。
- 如做实时功能，必须用真实运动/状态回放验证，不能只看静态页面或单次 API `OK`。

### API 客户端

- 有缓冲分帧、超时、重连、最大消息保护和异常 JSON 处理。
- 用 `uuid` 关联响应，用 `task_id` 记录任务，但以状态机判定完成。
- 同时处理 `response`、`callback`、`notification`；通知丢失不破坏流程。
- 每台机器人设置单一运动命令写入者；多客户端只读/控制边界明确，禁止并发发送导航和 `joy_control`。
- 对 `/api/request_data` 的订阅建立、重复请求、频率修改和断线清理做受控验证；没有文档证据时不调用旧版 `switch=off`。
- 断连、急停、非零错误码和状态超时均走失败关闭路径。
- `set_params` 后回读；地图切换/软件重启后重连并重新确认状态。
- 直接遥控有 0.5 秒看门狗和零速停止逻辑。

### 视觉/抓取

- 记录实际相机索引、分辨率、棋盘格规格、机械臂 IP、RK IP、tag family/size/ID 和通信方式。signal 模式还要记录 `camera_ip` 与插件 signal/Lua `sid`；Modbus 模式要记录实际 slave。
- 内参、手眼标定均采集足够多且分布合理的姿态。
- 插件页稳定识别生产标签；如经授权运行 `run_test`，先处理其固定测试 ID 1 与生产 ID 的差异，且测试结束后明确确认 `run_test=false`。
- 重做并记录当前安装对应的 offset；验证误差在要求内。
- 先空载、低速、无障碍地验证接近、抓取、返回和急停，再带真实物体。
- 不把示例 `cell2tag`、IP、tag ID 或截图参数当成生产配置。

## 14. 明确的未知项

原始交付资料没有提供（`vendor/` 中后来拉取的公开代码不能自动填补整机现场信息）：

- LM3-UP 整机级 ROS 节点、topic、service、launch 文件或 UP 导航栈源码；乐白公开 ROS 仓库只覆盖机械臂侧。
- SSH 用户、系统密码、服务名、文件系统部署路径。
- WATER TCP 消息分帧/终止规则、连接数、超时和心跳协议。
- `/api/request_data` 在当前协议中的停止订阅、重复请求、频率修改和断线后服务端清理语义；多客户端的控制权、优先级和并发写入仲裁。
- 完整 `error_code` 位定义。
- 相机设备节点、插件版本/日志、AprilTag 输出坐标系与旋转定义。
- `offset`/`cell2tag` 的变换方向、平移/旋转单位，以及 `good_pose` 与 `verify` 的 2 mm 要求是逐轴还是距离范数、是否包含旋转误差。
- 反光贴定位的传感器/识别原理、环境光和材质限制、25 cm 测量基准、三个 offset 的单位/方向，以及重复停靠误差与失败判据。
- 生产级抓取的碰撞模型、力限值、夹取成功判定和恢复策略。
- 当前现场设备的真实固件、网络、地图和标定状态。

遇到这些问题时，应请求现场信息或读取设备当前状态，不得从截图、文件时间或示例值继续推断。

## 15. SDK 仓库规则与当前结论

### 15.1 初始化

```powershell
& .\scripts\manage-sdks.ps1
& .\scripts\manage-sdks.ps1 -VerifyOnly
```

不要使用全仓 `git submodule update --init --recursive`：云迹旧版 `open-api` 上游包含缺失 `.gitmodules` 映射的 `docs/.vuepress/dist` Git link。脚本只显式递归初始化合法的 `lebai-sdk.rs/proto/lebai-proto`。

### 15.2 选型

- LM3 正式主入口：`vendor/lebai/sdk/lebai-sdk`；Python 使用 `pylebai`，控制器软件要求至少 3.1.5。
- UP 正式依据：交付 `water_api手册.pdf`。没有发现云迹官方公开的 WATER/UP SDK 或 ROS 驱动。
- ROS：`vendor/lebai/ros/lebai-ros-sdk` 的 `main` 是文档分支，代码在 Humble/Jazzy/Lyrical 等发行版分支；不包含 UP/Nav2/WATER。
- RK 视觉：结合 `vendor/lebai/integrations/plugin`、RK 指南和交付 LBD 使用。

### 15.3 不能直接投产的上游代码

- `vendor/lebai/lerobot/lerobot_lebai` 固定版本的 `lebai.py:76` 存在 Python 语法错误；还会在连接/断开时自动启停系统，没有校准、动作限幅、等待完成、碰撞或底盘动作空间。只能作为重写适配器的参考。
- `vendor/yunji/integrations/yunji_agv_skill` 由乐白而非云迹发布；监听线程和同步请求会在同一 socket 上竞争 `recv`，并硬假设换行分帧。WATER 手册没有给出这一分帧保证，不能直接作为生产控车层。
- `vendor/yunji/cloud/open-api` 是旧机器人服务生云端业务 API，不是 UP/WATER 控制 SDK。
- `vendor/lebai/integrations/OpenClawSkill` 是智能体工具封装，不是安全边界；不得让模型直接调用运动、关机、解除急停或破坏性配置接口。

详细分级、许可证和修复策略见 `docs/SDK与子模块.md` 与 `vendor/README.md`。

## 16. 手机遥操与 VLA 数据采集

### 16.1 当前实现边界

- 实现目录为 `teleop/`，包含 Android 原生客户端、HarmonyOS 原生客户端、Python 安全桥、协议和 LingBot 数据映射。
- Android/HarmonyOS 在启用姿态模式前都必须确认手机存在硬件陀螺仪；缺失时直接禁用。通过门禁后读取系统融合后的 Rotation Vector，以显式归零后的相邻姿态增量控制 LM3 TCP `Rx/Ry/Rz`；原有触屏轴键继续控制 `X/Y/Z`。标准握持为屏幕朝上、手机顶部指向机器人基坐标 `+X`，固定映射 `[tcp_rx,tcp_ry,tcp_rz]=[phone_y,-phone_x,phone_z]`，但首次使用仍必须在模拟器逐轴验向。手机 IMU 只能可靠提供 3DoF 旋转，禁止把它描述为能测量平移的完整 6DoF。
- `pose.sample` 只接受 `frame=phone_calibrated`、`mapping=tcp_orientation` 的旋转增量。每个新的 `calibration_id` 首帧只建立基线不运动；低置信度、传感器陈旧、时间倒退/间隔异常、非有限值或姿态跳变都必须失败关闭。
- UP 底盘不属于首版动作空间。机械臂遥操或 VLA 执行前，必须由独立确定性流程确认底盘停止并锁定。
- LingBot 推理应运行在外部 NVIDIA/CUDA 主机；没有证据证明公开实现可以直接部署到 RK3588S2。

### 16.2 启动和真机门槛

- 安全桥默认只能启动模拟后端并监听 `127.0.0.1`。
- 真机必须同时显式选择 hardware 后端、给出机器人 IP、设置认证令牌、配置并确认实测的非空 TCP 位置工作空间、TCP `Rx/Ry/Rz` 姿态中心/逐轴容差和六轴限位，并再次确认底盘锁定；`workspace_configured`、`orientation_configured`、`joint_limits_configured` 或任一其他门槛缺失时都应启动失败。
- 桥接器不得调用 `start_sys()`、`stop_sys()`、解除急停、关机或禁用限位。真机上使能和急停复位由现场人员通过受控流程完成。
- `movej`/`movel` 为异步运动缓冲接口，不能按模型帧率无限写入。连续遥操优先使用受限 `speedl` 或经真机验证的单槽动作执行器，并在 300 ms watchdog、反馈停滞或异常时调用 `stop_move()`。

### 16.3 协议和客户端规则

- 唯一协议标识为 `lm3-teleop.v1`；完整 schema 见 `teleop/PROTOCOL.md`。
- 同一时刻只有一个有效控制租约。消息序号、时效、有限数值、deadman、租约、机器人状态、底盘锁定和工作空间必须由服务端重新检查。
- Android/HarmonyOS 都必须在松手、页面不可见、App 后台、WebSocket 错误和断连前尽力发送 `motion.stop`，并在本地立即清零控制状态。
- 客户端最多 20 Hz 发送运动命令；300 ms 内没有新的有效 deadman 命令时，服务端必须失败关闭并停止。
- 陀螺仪模式必须由独立按住式 DEADMAN 授权，进入前显式归零；不能与触屏笛卡尔点动并发。服务端必须根据传感器毫秒时间计算角速度并再次限幅，不能直接执行手机提交的绝对姿态。
- 共享令牌不能写入源码、Git、URL 查询参数或普通日志。开发明文 WS 只能用于隔离局域网，生产使用 WSS/TLS 和设备级凭据。

### 16.4 数据和模型规则

- 原始 episode 必须保留命令、陀螺仪 `calibration_id`/传感器时间/原始旋转增量、实际关节/TCP/夹爪反馈、服务端时间、相机时间、网络间隔、钳制/watchdog/异常标志和任务文本，并生成 `manifest.sha256`。
- 训练 `action` 优先使用下一时刻实际到达的 `[q1..q6, gripper]`，不能把被拒绝或钳制的手机命令当作监督真值。
- 导出 LeRobot v3 后必须检查时间单调性、图像同步偏差、缺帧、维度和 normalization statistics，再进入 LingBot 后训练。
- 模型验证顺序固定为：离线回放 → 影子模式 → 固定底盘低速空载 → 小工作空间单任务 → 扩大任务。任何阶段失败都不得跳级。

详细架构、数据字段和验证阶梯见 `docs/手机遥操与VLA数据采集.md`。
