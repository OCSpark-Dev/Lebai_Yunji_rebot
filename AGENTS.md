# AGENTS.md

本仓库的完整协作与安全规则统一维护在 [CLAUDE.md](./CLAUDE.md)。

所有在本仓库工作的 Agent 必须：

1. 在分析、修改或运行代码前完整阅读并遵守 `CLAUDE.md`。
2. 将 `CLAUDE.md` 视为本文件引用并纳入的仓库级指令。
3. 保持原始交付 PDF、DOCX、LBD 和 ZIP 不变。
4. 遵守其中的真机运动、安全、子模块和验证边界。
5. 如本文件与 `CLAUDE.md` 出现不一致，以更严格、更安全的要求为准。

不要在本文件复制整套规则；规则更新应集中修改 `CLAUDE.md`，避免两份文档漂移。
